apiVersion: krepe.io/v1
kind: Krepe
imports:
  files:
    - deployment.yaml
    - service.yaml
    - ingress.yaml
pipelines:
  mypipeline:
    - function: set-labels
      configMap:
        foo: bar
  badpipeline:
    - function: jsonpatch
      configMap:
        op: add
        path: /foo/bar/baz
        value: 1
